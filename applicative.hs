
-- สำหรับ implement pure อาจจะใช้ ค่า default value สำหรับ r
-- ส่วน <*> ต้องผสม r สองตัวจาก (r, a -> b) และ (r, a) ให้กลายเป็น r ตัวเดียวจากนั้นจึงใช้ฟังก์ชัน (a -> b) กับค่า a โดยวิธีการรวม r จะขึ้นอยู่กับชนิดของมัน 
-- เช่นหากเป็น string อาจจะต่อ string  หากเป็นเลข อาจจะ + เลข

